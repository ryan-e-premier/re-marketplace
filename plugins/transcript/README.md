# transcript

Open the current Claude Code conversation transcript in nvim via a tmux
popup.

## Command

```text
/stn:transcript
```

Formats the current session's JSONL transcript to readable markdown and opens
it in an nvim buffer. Press `Enter` or `q` to close.

## Requirements

- nvim
- tmux
